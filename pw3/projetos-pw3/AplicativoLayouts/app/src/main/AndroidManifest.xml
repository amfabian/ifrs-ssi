<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.edu.ifrs.aplicativolayouts">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AplicativoLayouts">
        <activity android:name=".GridActivity" android:parentActivityName=".MainActivity"/>
        <activity android:name=".FrameActivity" android:parentActivityName=".MainActivity"/>
        <activity android:name=".Relative_Activity" android:parentActivityName=".MainActivity"/>
        <activity android:name=".Linear_Activity" android:parentActivityName=".MainActivity"/>
        <activity android:name=".Constraint_Activity" android:parentActivityName=".MainActivity"/>
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>